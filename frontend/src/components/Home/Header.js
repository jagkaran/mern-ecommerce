// components/Home/Header.js
// Thin re-export. The Header is now a folder (Header/index.js + child
// components + hooks). Existing imports `from "./components/Home/Header"`
// keep working through this file.

export { default } from "./Header/index";
