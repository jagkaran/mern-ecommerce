# react-scripts@5 pins ajv@6, yaml@1, and typescript@4 internally.
# Without this flag npm rejects the lock file due to peer dep conflicts
# with newer transitive versions. This keeps installs consistent across
# local dev, CI, and Render.
legacy-peer-deps=true
